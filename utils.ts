import { wordPracticeType } from "sharedInterfaces";

export const wordPracticeBasicTypes: wordPracticeType[] = ["multiple choice game", "type the answer game", "recognize it by the definition game"];
